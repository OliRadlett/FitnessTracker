# Services CODEMAP

> Each service follows the signature `(db: AsyncSession, user_id: UUID, ...)`. Services don't use FastAPI DI.

| File | Responsibility | Key Functions |
|------|---------------|---------------|
| `auth.py` | JWT creation/validation (SEC-07: `jti` per token, Redis logout denylist checked in `get_current_user`), OAuth provider config, single-use opaque OAuth state (SEC-02: 5-min Redis TTL, atomic GETDEL consume, provider-bound) | `create_access_token()`, `get_current_user()`, `get_current_token_data()`, `mint_oauth_state_token()`, `consume_oauth_state_token()`, `denylist_token()` |
| `connection_health.py` | Connection health state machine (BUG-072): single hardened token-refresh path (`SELECT … FOR UPDATE`, immediate commit of rotated tokens), 5-min proactive expiry leeway, `force=True` retry-once after mid-sync 401/403, typed error classification, `needs_reauth` marking. SYNC-03: `handle_sync_http_error()` optionally takes `refresh_client` for forced-refresh recovery (returns True) else reauth + banner, else transient tally | `refresh_connection()`, `mark_connection_reauth()`, `reset_connection_health()`, `handle_sync_http_error()` |
| `strava/` package (`sync.py`, `linking.py`, `webhooks.py`, `webhook_queue.py`) | Strava activity sync, activity↔lifting linking (`linking._match_score()`), webhook handling, route sync | `sync_activities()`, `link_activity_to_lifting_sessions()`, `handle_strava_event()`, `sync_strava_routes()` |
| `wahoo.py` | Wahoo activity + route sync | `sync_wahoo_activities()`, `sync_wahoo_routes()` |
| `komoot.py` | Komoot route sync | `sync_komoot_routes()` |
| `merge_service.py` | Activity dedup/merge engine, activity↔route linking | `find_duplicate_activity()`, `merge_activity()`, `link_activity_to_route()` |
| `route_service.py` | Route CRUD, dedup/merge (proximity + distance + name + shape scoring) | `create_or_merge_route()`, `get_routes()`, `delete_route()` |
| `lifting.py` | Session/set CRUD, PR detection (Brzycki), volume calculation, activity linking, live-session support (`get_active_session()` = latest `started_at IS NOT NULL AND ended_at IS NULL`). Live-sync idempotency: `create_session()` dedupes on `live_key`, `add_set()` dedupes on `(session_id, client_id)` — retries/concurrent flushes never duplicate. Implausible-duration guard (≥3h, `MAX_PLAUSIBLE_SESSION_DURATION_SECONDS`): `update_session()` + both link paths default to the linked Strava activity's time via `apply_strava_duration_fallback()`; the weekly `cleanup_old_data` heals pre-existing rows. FL3: %e1RM load prescription from PR/recent sets | `create_session()`, `get_active_session()`, `add_set()`, `_check_and_record_pr()`, `resolve_exercise_e1rm()`, `suggest_strength_load()`, `apply_strava_duration_fallback()`, `session_span_implausible()` |
| `whoop.py` | Whoop sync — cycles/recovery, sleep, weight, workout enrichment. Strength workouts with no Strava match are attached to live-tracked LiftingSessions by time overlap (`match_whoop_workout_to_lifting_session()`, ≥50% of shorter window; dedup via `whoop_workout_id`); used by both `sync_whoop_workouts()` and backfill | `sync_whoop_cycles()`, `sync_whoop_sleep()`, `sync_whoop_workouts()`, `match_whoop_workout_to_lifting_session()`, `backfill_whoop_data()`, `_backfill_missing_recovery()`. Weight sync updates the canonical profile reference weight (QW1) |
| `withings.py` | Withings scale sync — grouped `getmeas` weigh-ins decoded (`value×10^unit`), per-metric `RANGE_VALIDATORS` (bad metric → None, bad weight → drop group), BMI derived from same-group height, >3kg day-jumps logged, upsert `(user_id, date, "withings")`. Sync updates the canonical profile reference weight (QW1) | `sync_withings_measurements()`, `group_measurements()`, `decode_measure_value()`, `get_withings_connection()`, `refresh_if_needed()` |
| `events.py` | Event domain logic (B-12, extracted from `api/events.py`): countdown/taper enrichment, result summary formatting, result recording + `event_result` notification | `enrich_event()`, `format_result_summary()`, `get_event_for_user()`, `record_event_result()` |
| `exercise_db.py` | Built-in exercise database with aliases, normalisation, categorisation (static fallback). **Weight convention**: bilateral DB/dual-handle moves log PER implement — `PER_ARM_EXERCISES`, `weight_convention()` (`per_arm`/`total`), `is_per_arm_exercise()`, `looks_like_combined_weight()` (2×-median check). Ambiguous generics (Bicep Curl, Shrug) stay `total` | `normalise_exercise_name()`, `search_exercises()`, `weight_convention()` |
| `analytics.py` | Feature 3/B-15 deterministic athlete model: six observed-coefficient insights over 90d (Pearson + band means, min-sample guards), nightly upsert via `compute_all_insights()` | `compute_all_insights()`, `insight_recovery_cost()`, `insight_sleep_performance()`, `insight_load_readiness()`, `insight_power_norms()`, `insight_pr_clustering()`, `insight_tsb_peak()` |
| `video_analytics.py` | Lifting-video weekly maintenance (B-27/B-29/B-30/B-31-TSS): per-exercise aggregation upsert, injury-risk HealthAlerts from form deviations, RPE calibration + read-time `calibrated_rpe_for()` | `aggregate_video_analyses()`, `flag_injury_risks()`, `recalibrate_rpe()`, `calibrated_rpe_for()` |
| `vbt.py` | Velocity-based training (§3.18): load-velocity line fit over analysed sets, estimated 1RM from the minimal-velocity-threshold crossing (Squat 0.30 / Bench/Deadlift 0.15 / Press 0.20 m/s) + confidence. Pure functions | `load_velocity_profile()`, `mvt_for()` |
| `exercise.py` | DB-backed exercise library — CRUD, search, normalisation. Falls back to static `exercise_db` when DB is empty. Search results carry `weight_convention` (`per_arm`/`total`) for the logging UI | `search_exercises()`, `create_exercise()`, `update_exercise()`, `delete_exercise()`, `normalise_exercise_name()` |
| `cycling/` package (`tss.py`, `training_load.py`, `power_curve.py`, `vo2max.py`, `zones.py`, `power_profile.py`, `prs.py`) | TSS calc, CTL/ATL/TSB (τ=42/7, `1−e^(−1/τ)`), power curve from streams, power zones (7 Coggan) + LTHR zones, FTP estimation tiers + Riegel, VO2max (ACSM/Uth) + decoupling, W/kg percentile norms, PR detection/check. QW4: `auto_compute_tss_for_activity()` stamps `Activity.tss_source` ('power'/'hr'); `infer_tss_source()` is the conservative backfill heuristic (mirrors migration 062 SQL) | `compute_training_load()` (optional ctl/atl-day override, FL1), `training_load_for_user()` (fitted taus w/ 42/7 fallback, FL1), `sync_profile_reference_weight()` (canonical bodyweight, QW1), `compute_power_curve_from_streams()`, `auto_compute_tss_for_activity()`, `auto_compute_hr_tss_for_activity()`, `infer_tss_source()`, `estimate_vo2max()`, `estimate_ftp_from_power_curve_detailed()`, `percentile_wkg_at()` |
| `charts.py` | Chart generation — queries DB, returns ChartData dataclasses. Series support `y_axis` (secondary axis); ReferenceAreas support `y_axis`. 32 registered charts (see `api/charts.py` `CHART_REGISTRY`) incl. `ramp_rate`, `wkg_power_curve`, `power_duration_percentile`, `consistency_heatmap`, `sleep_consistency`, `strength_balance`, `body_composition_trend` (Withings BIA multi-series + recomposition insight), `resting_hr_trend`, `respiration_trend`, `whoop_recovery_trend` | `ChartService` class with methods per chart type |
| `polyline_utils.py` | Polyline encode/decode, Haversine distance, provider conversions | `decode_polyline()`, `encode_polyline()` |
| `gpx.py` | GPX 1.1 generation and parsing | `generate_gpx()`, `parse_gpx()` |
| `workout_planner.py` | Workout zone computation, target planning, route matching. Readiness caps max zone at z2 when legs are loaded (CD1, opt-in flag) | `compute_workout_zones()`, `plan_workout()`, `find_matching_routes()`, `get_readiness_recommendation()` |
| `session_analysis.py` | Post-session ride and lifting analysis | `analyze_lifting_session()`, `analyze_ride()` |
| `activity_context.py` | **§1.3** Precomputed ride-context cache — immutable ride analytics (power zones, decoupling, climbing, top speed, TSS breakdown) computed once at Strava sync/backfill/**webhook-create** into `Activity.context` (JSONB) so `/activities/{id}/context` avoids a full `analyze_ride` + stream recompute per expanded-card render. FTP staleness: readers recompute if `profile.ftp_watts` moved since the snapshot (`should_recompute_for_ftp`). Load context (ATL/CTL/TSB) deliberately NOT cached (moving window). Snapshots `tss_source`; readers accept a live-column override for pre-migration caches (QW4) | `compute_activity_context()`, `ensure_activity_contexts()`, `ensure_activity_contexts_by_id()`, `compute_top_speed()`, `ride_context_from_analysis()`, `context_to_ride_metrics()`, `should_recompute_for_ftp()` |
| `deficiency.py` | Weakness/deficiency analysis — strength standards, Big-3 ratios, push/pull balance, VO2max/FTP mismatch, decoupling, zone distribution | `analyze_deficiencies()`, `level_for_ratio()`, `evaluate_big3_ratios()`, `classify_push_pull()`, `evaluate_push_pull_ratio()` |
| `nutrition.py` | Ride fuel planning — carb/hydration/sodium targets by duration×IF, timed schedule generation, plan CRUD | `generate_fuel_plan()`, `compute_fuel_targets()`, `estimate_intensity_factor()`, `_build_during_ride_schedule()`, `update_fuel_plan_actuals()` |
| `llm_analysis.py` | LLM-powered analysis via Gemini (cycling, activity, lifting, health, event). Weekly + session contexts include `_big_lift_pbs()` — all-time best est. 1RM per Big-3 lift with achieved date, for historical strength context alongside 4-week `recent_prs`. QW5: weekly load context fixed (`tss_fetch_start`); weekly prompt defers alert/deficiency re-diagnosis to deterministic surfaces (RM2) | `compile_cycling_stats()`, `analyze_with_gemini()`, `run_llm_analysis()`, `_big_lift_pbs()`, `compile_activity_context()`, `analyze_activity_with_gemini()`, `run_activity_ai_analysis()`, `compile_lifting_session_context()`, `analyze_lifting_session_with_gemini()`, `run_lifting_session_ai_analysis()`, `compile_health_stats()`, `analyze_health_with_gemini()`, `run_health_ai_analysis()`, `compile_event_stats()`, `analyze_event_with_gemini()`, `run_event_ai_analysis()` |
| `weather.py` | Open-Meteo weather (no API key) — cached current/forecast/historical fetches, WMO mapping, bad-weather checks, activity weather tagging | `get_current()`, `get_forecast()`, `get_historical()`, `tag_activity()`, `tag_recent_activities()`, `resolve_user_coords()`, `_wmo_code_to_conditions()`, `degrees_to_compass()`, `is_bad_weather()`, `cache_coords()` |
| `segments.py` | **§3.13** Ride segment analysis — climb segments detected from route polyline + elevation profile (`detect_climb_segments`: sustained ≥3% climbs, ≥30m gain, ≥150m length, Strava-style `climb_category`), distance-aligned per-ride efforts via velocity-stream integration (`compute_effort_for_window` → elapsed/power/HR/speed/VAM), route sync builds `Segment` + `SegmentEffort` with PR totals | `sync_route_segments()`, `recompute_all_user_segments()`, `detect_climb_segments()`, `climb_category()`, `compute_effort_for_window()`, `list_segments()`, `get_segment_leaderboard()` |
| `training_plan.py` | Plan builder — mixed-week template generation, non-destructive day upsert by day_date, event linkage + auto-taper (linear 100%→40% ramp), plan CRUD, weekly view (Phase 5B): Monday-aligned `get_plan_week()` with readiness (reuses zones CTL/ATL/TSB logic), best-effort weather/bad-weather enrichment (today±7d window), batched actual activity/lifting lookups, route matches on cycle days via `find_matching_routes` (max 3); targeted single-day partial `update_plan_day()`. `planned_focus` is the single session-type field (merged from old focus + session_type in Phase 8) | `_generate_plan_days()`, `save_plan_days()`, `link_event_and_apply_taper()`, `create_plan()`, `update_plan()`, `generate_plan()`, `list_plans()`, `get_plan()`, `delete_plan()`, `get_plan_week()` (adds per-day `targets_stale`, FL1), `update_plan_day()`, `reschedule_plan_day()` (FL2, 409 on occupied), `substitute_plan_day()` (FL2), `get_unplanned_actuals()` (FL2), `refresh_cycle_targets()` + `targets_stale_for_day()` (FL1, FTP propagation), `refresh_strength_targets()` (FL3, %e1RM basis), `PlanDayConflictError` |
| `adaptive.py` | **§3.11** Adaptive weekly training advice — combines TSB trajectory (`compute_training_load`), latest recovery score, plan conformity (5C), active health alerts and top deficiency into a weekly "shift" (volume/intensity/rest) with one-tap day apply actions (scale ×0.85/×1.08 or rest-swap on next ≤3 training days, applied via `update_plan_day` PATCH). Pure inference in `derive_adaptive_advice()` (unit-tested in `tests/test_adaptive.py`) | `generate_adaptive_suggestions()`, `derive_adaptive_advice()`, `_decide_intensity()`, `_actions_for()`, `_upcoming_training_days()`, `_off_pace_performance_goals()` (QW6 — off-pace ftp/weekly_tss/1rm/sessions goals vote raise when conformity ≥70 + no health block; met/ahead goals deliberately add nothing), `cross_sport_fatigue()` (CD1 — legs/cycling loaded flags ease the other sport), `strength_readiness()` (CD2 — recovery/HRV-gated ×0.9/×1.05 strength modulation) |
| `goal_metrics.py` | Semantic metric registry (Phase 6) — key → label/unit/required-filters/resolver. 13 metrics: ftp_watts, body_weight, estimated_1rm(+exercise), weekly_sessions(+sport), monthly_distance_km(+sport), weekly_tss(+sport), vo2max, squat/bench/deadlift_bw_ratio, big3_total, resting_hr, hrv_ms | `METRIC_REGISTRY`, `resolve_metric()`, `list_metrics()`, `validate_metric_filters()` |
| `goals.py` | Goal state engine (Phase 6): direction derived from starting_value vs target (no column), uniform status transitions (abandoned is terminal; reactivate suppresses re-expiry), sign-safe alignment score clamped 0–200, check-in recording | `compute_goal_state()`, `update_goal_status()`, `alignment_pct()`, `derive_direction()`, `record_manual_check_in()`, `list_check_ins()`, `record_all_check_ins()`, `reactivate_goal()` |
| `health_analysis.py` | §3.12 Health alert analysis: composite signals (overtraining, injury risk, illness), three regeneration signals (`performance_decline` via FTP-history drop, `sleep_consistency` via nightly-duration σ, `resting_hr_elevation` vs 30-day baseline), active-alert upsert + in-app notify, per-user prefs (`User.health_preferences` JSONB: disabled list / snoozed-until dates / threshold overrides). QW7: upsert honors disabled/snoozed for ALL types, notifies link to `/health`, recently-dismissed alerts reactivate silently (7-day quiet, no re-push) | `analyze_overtraining()`, `analyze_injury_risk()`, `analyze_illness()`, `analyze_regeneration_signals()`, `upsert_alert()`, `alert_suppressed_by_prefs()`, `get_health_preferences()`, `set_health_preferences()`, `_analyze_performance_decline()`, `_analyze_sleep_consistency()`, `_analyze_resting_hr_elevation()` |
| `preferences.py` | §3.6 UI preferences: `User.preferences` JSONB (NULL = defaults). Merged-get + per-field validated partial-set (RAISE ValueError → 422), no commit (caller owns txn) | `get_preferences()`, `set_preferences()` |
| `projections.py` | Projections & success prediction (Phase 7): OLS regression on day-offsets, trend extrapolation to target crossing, badge classification (On Track / At Risk / Unlikely / Not enough data), TSB projection using planned TSS with EMA (CTL τ=42, ATL τ=7), metric trend for all 13 registry metrics (FTP/weight/HR/HRV/1RM/TSS/VO2max/bw-ratios/big3/sessions/distance) | `linear_regression()`, `project_to_target()`, `success_badge()`, `tsb_projection()`, `compute_goal_projection()`, `compute_metric_trend()`, `compute_tsb_projection()` |
| `cache.py` | Redis-backed response caching + distributed locks | `cached()`, `redis_lock()` |
| `app/metrics.py` (top-level, not `services/`) | Prometheus counters for sync outcomes (`SYNC_RUNS`, `CONNECTION_REAUTH`, `WEBHOOK_EVENTS`) | module-level counters |
| `strava/webhook_queue.py` | Async Strava webhook event processing (drain `strava_webhook_events` oldest-first with attempts/error + retry-then-fail) + weekly reconciliation against the Strava list (heals missed deletes/renames in a bounded window) | `process_pending_strava_events()`, `reconcile_strava_activities()` |
| `notifications.py` | In-app notifications (Feature 1): preference-gated (`User.notification_preferences`, NULL = all on), dedup-keyed idempotent creation, no commit (caller owns txn). Wired from health alerts (`upsert_alert` on new alert), PRs (`lifting._notify_pr`), goal milestones + plan reminders + weekly plan reviews (`plan_review`, scheduler tasks) | `notify()`, `get_notification_preferences()`, `set_notification_preferences()` |
| `push.py` | §3.8 Web Push (VAPID/RFC 8292): per-user subscription CRUD, best-effort delivery to all device subs with failure tracking per endpoint + pruning dead endpoints (410/404 or ≥5 consecutive failures). Skips silently when VAPID keys unset. Called from `notify()` so pushes ride the same call sites as in-app notifications; never raises | `register_subscription()`, `unregister_subscription()`, `list_subscriptions()`, `send_push_to_user()` |
| `data_export.py` | §3.9 Full user data export: serializes all 24 per-user collections into a single JSON document for GDPR portability. Uses generic column introspection (`sa_inspect().mapper.columns`) with explicit `selectinload` for nested children (activity streams/sources, lifting sets, warmup steps, goal check-ins, plan days, route sources/quality/tag+collection ids). Excludes global `Exercise` seeds (user_id=NULL) and `StravaWebhookEvent` (no user FK) | `build_full_export()` |
| `pdf_report.py` | ReportLab PDF reports (A4, shared styles + `_build_summary_table`): **weekly** (activities, recovery/sleep, PRs), **monthly** (weekly breakdown, activity list), **race-day prep / §3.14** (event overview, TSB-readiness projection toward event date, plan conformity + observations, taper checklist, fuel plan from planned duration/IF 0.75 + body weight, race-week weather). All take `(db, user_id, ...)` and return PDF `bytes`; event report raises `ValueError` when the event is missing | `generate_weekly_report()`, `generate_monthly_report()`, `generate_event_report()` |
| `conformity.py` | Phase 5C plan conformity (5C model): per-day classification (done/pending/missed/extra) + weekly rollup, focus-conflict detection | `build_cycle_conformity()`, `build_strength_conformity()`, `classify_conformity()`, `score_component()`, `missed_conformity()`, `pending_conformity()` |
| `effort_estimator.py` | Route effort prediction (Martin power model): required power from grade/Crr/weight, effort score per route | `estimate_required_power()`, `estimate_effort()` |
| `route_quality_service.py` | Route quality scoring: completeness/popularity/surface/effort-match/terrain sub-scores → overall, stored on `RouteQuality` | `compute_overall_score()`, `compute_and_store_quality()` |
| `route_collection_rules.py` | Smart-collection rule evaluation (filter state → matching routes) | `evaluate_smart_collection()`, `evaluate_all_smart_collections()`, `validate_collection_rules()` |
| `fit_parser.py` | FIT file import parsing (Garmin/FIT binary → activity + streams) | `parse_fit_file()` |
| `llm_base.py` | Shared Gemini plumbing split out of `llm_analysis.py`: JSON-safe serialisation, Big-3 context, analysis storage, model calls. AI-01/03 guards: per-user 5/min budget + in-flight dedupe (`ai_generation_guard`, 429s), insufficient-context 400 (`ensure_context_sufficient`) | `_call_gemini()`, `_store_analysis()`, `_big_lift_pbs()`, `ai_generation_guard()`, `ensure_context_sufficient()` |
