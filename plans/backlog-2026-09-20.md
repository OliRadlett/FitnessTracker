# Implementation Backlog — approved 2026-09-20

> **Origin**: item-by-item triage of all unimplemented work found in the planning docs
> (`plans/future-enhancements.md`, `plans/routes-redesign.md`,
> `plans/modal-inference-expansion.md`, `plans/feature{2,3,5,6,7}.md`,
> `docs/audits/SYNTHESIS-2026-09-20.md`). Every item below was explicitly approved.
> Deferred / dropped / separately-tracked items are listed at the bottom, not here.
> Source docs carry a pointer back to this file; scope detail stays in the source doc.

**Execution order**: Phase 0 → 1 → 2 → 3 → 4 → 5. Dependencies noted per item
(`needs B-xx` = build after). Each phase is an independent branch + PR (`main` → `prod` to ship).

**Implementation status (2026-09-20): Phase 0 (B-01…B-05, B-37) and Phase 1
(B-06…B-09) are committed (`72a4138`, pushed to `main`) — each gated by
compile + targeted integration tests on a fresh test DB.**

**Update 2:** Phase 3 musical-chairs check — B-19 (`/notifications` page) was
already built (verified, no work needed). B-20…B-25 built: weight EMA/deltas,
post-race retrospective (+ `GET /events/{id}/retrospective`), weekly digest
task (`weekly_summary`/`streak_milestone`/`deload_started`), routes infinite
scroll + discovery tips, light theme (token migration + toggle).

**Update 3:** Phase 4 (B-26…B-31) built: 3 video trend charts + registry,
`aggregate_video_analyses_weekly` (Sun 7:30 — aggregation + injury flags +
RPE calibration + TSS backfill), VideoProgressTab (Bank/Progress tabs),
injury-risk HealthAlerts, `calibrated_rpe` on process-status, lifting
`estimated_tss` (migration 064, compute-on-write + schema), `combined_training_load`
chart on Analytics, form-quality deficiency items. Warmup-effectiveness analysis
explicitly deferred (no warmup-adherence linkage exists to analyze).

**Update 4:** Phase 5 (B-32…B-36) built: GHCR names parameterised, monitoring
stack (Prometheus + Alertmanager + Grafana JSON, configs validated with
promtool/amtool) + DEPLOY.md section, Playwright CI job, `mutations.spec.ts`
(5 flows) + `routes-organize.spec.ts` (4 flows), E2E fixture crash fixes
(goal `metric`, adaptive-suggestions + analysis mocks, count header). Full E2E
baseline NOT green (broad pre-existing mock drift); fixed suites: routes (18),
organize (4), mutations (5). Also fixed live bugs found by E2E:
`planWeek?.days.find` crash (today + TodayTab), route-name column collapse.

**COMPLETE (2026-09-20): all of Phases 0–5 built and committed** —
`72a4138` (Phases 0–1) → `c2ec772` (Phase 2) → `ee23c69` (Phase 3 + batch) →
`d9cef5c` (Phases 4–5), all pushed to `main`. Remaining: POC go/no-go reviews
(need real-data judgment), deferred §3.13/B-45, separate §0.1, full-E2E
baseline rehab, warmup-effectiveness analysis.

---

## Phase 0 — Security & data-loss (do first; from audit SYNTHESIS Phase 0)

| ID | Item | Source | Notes |
|----|------|--------|-------|
| B-01 | Loopback-bind prod ports (5432/6379/8000 to `127.0.0.1`) | SEC-01 | `docker-compose.yml` + `docker-compose.prod.yml` |
| B-02 | Short-lived OAuth `state` + JWT revocation / real logout | SEC-02+07 | 30-day replayable token can `DELETE /account/delete` today |
| B-03 | Route-quality `selectinload(Route.sources)` | DATA-01 | Nightly task + `POST /quality/recompute` return `updated: 0` today |
| B-04 | Strava backlog drain + 401/403 → `needs_reauth` | SYNC-02/03 | Watermark stalls past 100-item backlog; revoked conns retried silently |
| B-05 | Move `@celery_app.task` decorator onto `analyze_cross_domain_weekly` | RMI-01/SYNC-01 | `backend/app/tasks/scheduler.py`; beat dispatches 2-arg helper with 0 args |
| B-37 | Fix streams-backfill `NameError` (`total = len(activities)` → `len(rows)`) | SYNC-04 (adjacent find during B-04) | `strava/sync.py` backfill; raises after committing all work, so task reports failure and returns no counts |

## Phase 1 — Correctness & trust

| ID | Item | Source | Notes |
|----|------|--------|-------|
| B-06 | Per-user Gemini cap + in-flight dedupe + insufficient-data guard | AI-01/03 | Stops uncapped spend + empty contexts stored as real analysis |
| B-07 | Onboarding reachability + timer fix | UX-01/02 | Profile/goal steps unreachable; timer yanks wizard to step 1 |
| B-08 | BUG-015 double-commit fix, per-file with test gate | §1.4 | 34 explicit commits vs `get_db` auto-commit; never bulk |
| B-09 | Server-side OAuth `redirect_uri` | BUG-025 | Backend returns it in authorize response; touches all providers |

## Phase 2 — Code health

| ID | Item | Source | Notes |
|----|------|--------|-------|
| B-10 | Dead-client fork: delete 8 fully-dead modules, migrate nearly-used back, rebuild CODEMAP | §2.1 | Record A/B decision first |
| B-11 | `openapi-typescript` codegen (`lib/api/types/generated.ts`) | §5.9 | needs B-10 (generate from survivors) |
| B-12 | Extract `services/events.py` | §4.2 | Before any further event work |
| B-13 | WeeklyTab prop split + shared monthly/yearly components | BUG-040/041 | 35 props + ~200 duplicated lines |

## Phase 3 — User-facing features

| ID | Item | Source | Notes |
|----|------|--------|-------|
| B-14 | Feature 2: sleep cards on Today + projection lines on FTP/VO2max/weight/1RM | feature2.md | Read-only surfacing of existing endpoints |
| B-15 | Feature 3: correlation engine + Analytics page | archive/feature3.md | 6 deterministic insights; unblocks B-16/B-17 |
| B-16 | Feature 5: `/today` brief + go/no-go review | feature5.md | needs B-15; isolated route, existing pages untouched |
| B-17 | Feature 6: prescriptions + go/no-go review | feature6.md | needs B-15 + B-16 |
| B-18 | Feature 7: AI overlays + go/no-go review | feature7.md | needs B-15; existing Gemini infra only |
| B-19 | `/notifications` page (history, filters, mark-all-read) | §3.5 | **Already built** (verified 2026-09-20 — no work needed) |
| B-20 | Combined weight tab (trend + EMA + deltas) | §3.1 | Repackages existing WeightPanel data |
| B-21 | Post-race retrospective | §3.3 | Prerequisite: activity↔event linking (subtask of this item) |
| B-22 | Weekly-summary / streak / deload notifications | §4.4 | Existing `notify()` infra |
| B-23 | Routes empty states + discovery tooltips | routes Phase 4 | — |
| B-24 | Routes infinite scroll + thumbnail preload | routes Phase 4 | — |
| B-25 | Light theme (CSS-var migration) | §3.15 | P3; schedule last in this phase |

## Phase 4 — Modal video intelligence (all remaining §14 boxes)

| ID | Item | Source | Notes |
|----|------|--------|-------|
| B-26 | `video_form/velocity/consistency_trend` charts in CHART_REGISTRY | §10.4 | Data already stored; registry + UI only |
| B-27 | `aggregate_video_analyses` weekly Celery task | Phase 6 | Populates `lift_video_analyses` per exercise |
| B-28 | VideoProgressTab (cross-video trends) | Phase 6 | needs B-26 + B-27 |
| B-29 | Injury-risk flags + `HealthAlert` entries | Phase 6 | From form deviations (valgus, rounding, …) |
| B-30 | RPE auto-calibration + weekly recalibration task | Phase 5 | Table exists; computation missing |
| B-31 | Phase 7: lifting TSS + unified load dashboard (+ deficiency/warmup) | Phase 7 | Velocity-fatigue input from B-30 |

## Phase 5 — Platform & testing

| ID | Item | Source | Notes |
|----|------|--------|-------|
| B-32 | Parameterise prod GHCR image names | Part 6 | Source from env |
| B-33 | Prometheus alert rules + Grafana board | Part 6 | 5xx rate, sync failures, queue backlog |
| B-34 | Playwright E2E job in CI | Part 6 | Against compose-up stack |
| B-35 | E2E mutation flows (live-lift, goals, tags/collections, GPX, deep-links) | Part 7 | Runs in B-34 |
| B-36 | Routes Vitest + Playwright coverage | routes Phase 4 | Fold into B-35 where overlapping |

---

## Decided otherwise (not in this backlog)

- **Deferred**: §3.13 cross-route climb clustering + distance alignment (needs research);
  BUG-045 secret rotation (manual ops task).
- **Dropped**: routes drag-drop tagging (chips + bulk cover it); routes keyboard shortcuts.
- **Separate case**: §0.1 land in-flight tree / stash investigation — handled independently.
- Audits (`docs/audits/*`) are point-in-time records and intentionally not edited;
  their P0s live above as B-01…B-07.
